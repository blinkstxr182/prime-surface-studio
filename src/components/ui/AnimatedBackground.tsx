// Animated lime background blobs intentionally disabled for the EVW-style
// clean minimal aesthetic. Returning null keeps the import sites working
// without rendering anything.
export function AnimatedBackground() {
  return null;
}
